按 `Ctrl+C` 后，在鼠标旁弹出迷你气泡，告诉你这次复制到底成功没有。

- **复制成功** → 绿色「复制成功」
- **复制失败** → 黄色「复制失败」

气泡向上飘动并逐渐淡出，约 0.85 秒消失，不打断操作。

## 下载

下载下方的 **copy-toast.exe**，双击运行即可，免安装、免依赖。

启动后常驻系统托盘：

| 操作 | 效果 |
| --- | --- |
| 左键单击 | 预览一次「复制成功」气泡 |
| 右键 | 预览成功 / 预览失败 / 退出 |
| 鼠标悬停 | 显示当前状态 |

> Windows 11 首次运行可能把新图标折叠进托盘溢出区（点 `^` 箭头展开），把它拖到托盘区即可常显。

## 它凭什么判断"复制成功"

不看有没有按下 `Ctrl+C`，而是看剪贴板是否**真的被写入**：

1. 低层键盘钩子（`WH_KEYBOARD_LL`）旁观按键，并调用 `CallNextHookEx` **原样放行**——不拦截、不吞键，前台程序照常完成复制；
2. 按下 180ms 后，用 `GetClipboardSequenceNumber()` 检查剪贴板序号有没有变化。

序号变了才算成功。所以不会出现"按了就说成功"的假反馈——按了但没复制到，会如实弹黄字。

（早先版本用 `RegisterHotKey` 是错的：它会把 `Ctrl+C` 独占吞掉，目标程序根本收不到按键，复制也就没有发生。）

## 特性

- 单文件 exe，约 12MB，**零第三方依赖**（仅 `ctypes` + `tkinter`）
- 常驻内存约 33MB，弹过若干次提示后稳定在 36MB 左右不再增长；空闲时 CPU 接近 0
- 不需管理员权限
- 气泡尺寸由字体度量自动计算，改文案或字号无需手调

## 从源码运行

需要 Python 3 且**带 tkinter**（python.org 官方安装包默认自带）：

```
py copy_toast.py                 启动
py copy_toast.py --once          预览成功样式
py copy_toast.py --once --fail   预览失败样式
```

## 已知限制

- 在终端里按 `Ctrl+C` 中断程序也会弹一次「复制失败」（剪贴板确实没变）。嫌吵就把 `WARN_ON_MISS` 改成 `False`。
- 以管理员身份运行的程序里的按键收不到，这是 Windows 的会话隔离机制。需要覆盖它的话，本工具也要以管理员身份运行。
- 部分应用（远程桌面、个别终端、Electron 应用）复制失败是静默的，这种情况能正确提示失败。

---

Windows 10 / 11 64 位。
